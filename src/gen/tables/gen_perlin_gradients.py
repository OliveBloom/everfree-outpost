import math
import sys
import time

N = 64

print('// AUTO-GENERATED by %s at %s' % (sys.argv[0], time.asctime()))
print('static GRADIENT_TABLE: [(f64, f64); %d] = [' % N)
for i in range(N):
    x = math.cos(i * 2 * math.pi / N)
    y = math.sin(i * 2 * math.pi / N)
    print('    (%.12f, %.12f),' % (x, y))
print('];')


